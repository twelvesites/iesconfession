name: Auto Instagram Posting

on:
  schedule:
    - cron: "0 * * * *"  # every hour
  workflow_dispatch:

jobs:
  post-confessions:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install system dependencies for Pillow
        run: |
          sudo apt-get update
          sudo apt-get install -y libjpeg-dev zlib1g-dev libfreetype6-dev liblcms2-dev libtiff5-dev tk-dev tcl-dev

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.12

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r auto-Instagram-posting-bot/requirements.txt

      - name: Run bot
        run: python3 auto-Instagram-posting-bot/bot.py
        env:
          FIREBASE_JSON: ${{ secrets.FIREBASE_JSON }}
