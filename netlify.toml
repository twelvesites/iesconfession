[build]
publish = "."
command = "echo 'No build command needed'"

[functions]
directory = "netlify/functions"

[secrets_scanning]
enabled = false
omit_paths = [
  "about.html",
  "analytics.js"
]

[dev]
[dev.environment]
# Environment variables for local development only
# Remove the hard-coded values from here and put them in your .env file
VERCEL_API_URL = "https://iestea-backend.vercel.app/api/confess"
VERCEL_ORIGIN_SECRET = "YOUR_SECRET_KEY_HERE"